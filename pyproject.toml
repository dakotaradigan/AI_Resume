[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
# Explicit selection keeps lint deterministic across ruff releases:
# pycodestyle errors, pyflakes, and import sorting.
select = ["E4", "E7", "E9", "F", "I"]

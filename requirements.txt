# Core Backend Dependencies
# Keep FastAPI/Starlette current: old pins ship Starlette versions with known
# CVEs (e.g. CVE-2024-47874 multipart DoS, fixed in Starlette 0.40).
# Upper bounds keep security patches flowing (minor/patch) while stopping an
# unreviewed breaking major from shipping on the next rebuild.
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
python-dotenv>=1.0.0,<2.0

# AI & LLM
anthropic>=0.40.0,<1.0

# Shared state
redis>=5.0.1,<7.0

# Vector Search (Phase 3 - RAG Pipeline)
qdrant-client==1.12.0
openai>=1.75.0,<2.0
tenacity==8.2.3  # Retry logic with exponential backoff

# PDF resume download (Phase E)
reportlab>=4.2.0,<5.0

# MCP endpoint: /mcp with a single data-only get_resume tool (Phase E)
mcp>=1.28.0,<2.0
